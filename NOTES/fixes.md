# Fixes

## FIX-001 — Status label layout stomping during long filenames
**Date**: 2026-04-12  
**Problem**: Single status label caused long filenames to push speed/ETA off screen or overlap.  
**Fix**: Split into two dedicated labels — `_filename_lbl` (wrapping) and `_meta_lbl` (single line). Added `wraplength=WRAP` (532px) so filenames wrap naturally without truncation.  
**File**: `app.py:120–135`

## FIX-002 — ffmpeg not found in distributed `.app`
**Date**: 2026-04-12  
**Problem**: Packaged `.app` shipped without ffmpeg, causing merge failures for split video+audio streams.  
**Fix**: Added `static-ffmpeg` to requirements and build pipeline. `_bundled_ffmpeg()` detects PyInstaller context and points yt-dlp at the bundled binary via `ffmpeg_location`.  
**File**: `app.py:55–59`, `build.sh`, `requirements.txt`

---
## Code Review DEC-100 Fixes (2026-04-12)

## FIX-003 — Thread-safety: StringVar accessed from background thread (D2)
**Problem**: `_worker()` called `self._quality_var.get()` and read `self._save_dir` from the daemon thread. tkinter is not thread-safe; this is a race condition.  
**Fix**: Both values are captured on the main thread inside `_start_download()` before the thread launches, then passed as arguments: `threading.Thread(target=self._worker, args=(url, quality_key, save_dir))`. Worker never touches tkinter state directly.  
**File**: `app.py:191–204, _worker signature`

## FIX-004 — No WM_DELETE_WINDOW handler — thread leak on close (D3)
**Problem**: Closing the window mid-download left the daemon thread running silently; partial files accumulated; Python process could hang on exit.  
**Fix**: `self.protocol("WM_DELETE_WINDOW", self._on_close)` registered in `__init__`. `_on_close()` sets the cancel event and polls via `_wait_and_close()` (100ms intervals, max 3 s) until the worker exits, then calls `self.destroy()`.  
**File**: `app.py:96, 148–164`

## FIX-005 — No URL validation — garbage input reached yt-dlp (D4)
**Problem**: Any string (phone number, file path, plain text) passed directly to yt-dlp, producing raw tracebacks as user-facing error messages.  
**Fix**: `_validate_url()` static method checks for `^https?://` regex before dispatch. `_friendly_error()` maps known yt-dlp error substrings to plain-English copy. `_ERROR_MAP` covers 12 common failure patterns.  
**File**: `app.py:40–50, 65–73, 175–181`

## FIX-006 — Playlist URLs silently downloaded all videos (D5)
**Problem**: Pasting a YouTube playlist URL downloaded every video in the playlist without warning, potentially filling the user's disk.  
**Fix**: `_probe_playlist()` runs `extract_flat` before download to detect playlist type. If count > 1, `_ask_playlist()` shows a native confirmation dialog (blocks worker thread via `threading.Event`, max 120 s timeout). User must confirm before download proceeds.  
**File**: `app.py:212–248`

## FIX-007 — Unpinned dependencies (D6)
**Problem**: `requirements.txt` had bare package names. A yt-dlp update could silently break all downloads.  
**Fix**: Pinned to tested versions: `yt-dlp==2026.3.17`, `customtkinter==5.2.2`, `static-ffmpeg==3.0`.  
**File**: `requirements.txt`

## FIX-008 — Hardcoded absolute path committed in .spec (D7)
**Problem**: `SaveThisVideo.spec` contained `/Users/goralski/...` path, making it non-portable and misleading as a committed artifact.  
**Fix**: Added `*.spec` to `.gitignore`. The spec is regenerated by `build.sh` on every run; there is no value in committing it. Created `.gitignore` covering `.venv/`, `build/`, `dist/`, `*.spec`, `__pycache__/`, `.DS_Store`.  
**File**: `.gitignore` (new)

## FIX-009 — Silent exception in _paste() (D8)
**Problem**: `except Exception: pass` swallowed clipboard errors with no user feedback.  
**Fix**: Replaced with targeted handler that shows `"Nothing on clipboard to paste."` in the meta label. Empty clipboard after stripping is also handled with the same message.  
**File**: `app.py:139–148`

## FIX-010 — Module-level side effects outside __main__ guard (D9)
**Problem**: `ctk.set_appearance_mode("system")` and `ctk.set_default_color_theme("blue")` were called at module level, initialising the Tk subsystem on any import.  
**Fix**: Moved both calls inside the `if __name__ == "__main__"` block alongside `_setup_logging()` and `App().mainloop()`.  
**File**: `app.py:306–309`

## FIX-011 — No version constant (D10)
**Problem**: No `__version__` or version string anywhere. Window title hardcoded. `.spec` had no bundle version.  
**Fix**: `APP_VERSION = "1.0.0"` defined at top of module. Window title set to `f"SaveThisVideo {APP_VERSION}"`. `build.sh` passes `--app-version` (via PyInstaller) when the notarization pipeline is active.  
**File**: `app.py:16, 89`

## FIX-012 — Raw yt-dlp error messages shown to users (S1)
**Fix**: `_friendly_error()` maps 12 known yt-dlp error substrings to plain-English copy. Unmatched errors have the `[extractor] id:` prefix stripped via regex before display.  
**File**: `app.py:65–73`

## FIX-013 — _on_done showed directory but not filename (S2)
**Fix**: `_hook` captures `d.get("filename", "")` when `status == "finished"` into `self._last_saved_path`. `_on_done()` reads it via `os.path.basename()` and shows it in `_filename_lbl`: filename on line 1, save directory on line 2.  
**File**: `app.py:275–287, 294–302`

## FIX-014 — Old filename label persisted at new download start (S3)
**Fix**: `_set_status(filename="", meta="Starting…")` in `_start_download()` now explicitly clears `_filename_var` before launching the worker.  
**File**: `app.py:200`

## FIX-015 — tkinter.filedialog import inside method (S4)
**Fix**: Moved `import tkinter.filedialog as fd` and `import tkinter.messagebox as mb` to module top-level imports.  
**File**: `app.py:10–11`

## FIX-016 — WRAP magic number with unexplained approximation (S5)
**Fix**: Replaced `WRAP = 532` with named constants: `WINDOW_W = 580`, `SIDE_PAD = 20`, `CTK_LABEL_PAD = 8`, `WRAP = WINDOW_W - SIDE_PAD * 2 - CTK_LABEL_PAD`. The math is explicit and self-documenting.  
**File**: `app.py:21–24`

## FIX-017 — No logging for production diagnostics (S6)
**Fix**: `_setup_logging()` attaches a `RotatingFileHandler` to `~/Library/Logs/SaveThisVideo/app.log` (2 MB max, 3 backups). Module-level `log = logging.getLogger("savethisvideo")` is used throughout. Logs: app start, download start/complete/cancelled/failed, app close. Does not log full URLs (privacy).  
**File**: `app.py:77–95, 98`

## FIX-018 — Pre-flight disk space check missing (S8)
**Fix**: `shutil.disk_usage(self._save_dir).free` checked before download starts. If < `DISK_WARN_BYTES` (500 MB), user sees friendly inline error with free space shown in MB. `OSError` (unmounted/inaccessible path) is silently swallowed — yt-dlp will surface it if it matters.  
**File**: `app.py:183–192`

## FIX-019 — Progress bar stayed at previous state between downloads (S9)
**Fix**: `_reset()` now calls `self._progress.set(0)` unconditionally, so the bar returns to zero on every terminal state (done, cancelled, error). `_on_done()` then sets it to 1.0 after reset to show completion.  
**File**: `app.py:303–309`

## FIX-020 — _save_dir stored as str, initialized from Path (S10)
**Fix**: `self._save_dir` is now typed and stored as `Path` throughout. `_trunc_path()` accepts `Path`. `_browse()` assigns `Path(path)`. yt-dlp `outtmpl` uses `str(save_dir / "%(title)s.%(ext)s")`. `_on_done` and `_start_download` use `self._save_dir` directly.  
**File**: `app.py:91, 152, 253`

## FIX-021 — Added cookies-from-browser dropdown
**Fix**: New `_cookies_var` StringVar and `CTkOptionMenu` on the cookie row. When not "None", yt-dlp's `cookiesfrombrowser` option is set in both `_probe_playlist` and `_worker`. Friendly error messages added for cookie-related failures.  
**File**: `app.py:59, 172-183, 334-335, 402-403`

## FIX-022 — macOS completion notification
**Fix**: New `_notify(title, message)` helper that shells out to `osascript`. Called from `_on_done()` after a successful download. Uses `subprocess.run` with 3s timeout; all exceptions are caught and logged at debug level.  
**File**: `app.py:108-120, 486-487`

## FIX-023 — H.264 preference checkbox
**Fix**: New `QUALITY_OPTIONS_H264` dict with parallel format strings that prefer AVC video. `_h264_var` BooleanVar drives a CTkCheckBox on the cookie row. Worker selects the H.264 format map when the flag is set and the quality is not Audio Only.  
**File**: `app.py:42-52, 188-193, 309, 386-390`

## FIX-024 — Clip section (Start/End time range)
**Fix**: New Clip section toggle + Start/End CTkEntry fields (hidden until toggle is on). `_parse_time()` accepts SS, MM:SS, and HH:MM:SS. Worker passes the range through `yt_dlp.utils.download_range_func` with `force_keyframes_at_cuts=True`. Validates empty/empty, End ≤ Start, and malformed formats inline before dispatch.  
**File**: `app.py:85-106, 196-229, 310-329, 424-425`

## FIX-025 — Re-downloading same video silently overwrote the prior file
**Fix**: Added `_unique_outtmpl()` helper that sanitizes the title, checks for collision in `save_dir`, and appends ` (1)`, ` (2)`, … until a free name is found. `%` chars in the literal path are escaped to `%%` so yt-dlp does not treat them as template field markers. Refactored `_probe_playlist` → `_probe_info` so the same probe call feeds both playlist detection and title lookup.  
**File**: `app.py:19, 459-499, 518-527`

## FIX-026 — UI restyle + restructure (DEC-022, DEC-023)
**Date**: 2026-04-15
**Fix**: Replaced the flat CTk form with a Spotify-inspired dark UI. Scope:
- New visual-tokens block (`COLOR_*`) at module top; all hex codes flow through tokens. `ctk.set_appearance_mode("dark")` pinned (previously "system").
- `_build_ui` rewritten — header strip, four labelled sections (PASTE A LINK / QUALITY / SAVE TO / OPTIONS), clip range revealed by toggle, bottom "Now Playing" dock with circle CTA.
- Quality `CTkOptionMenu` replaced by segmented 7-pill row + new `_select_quality(key)` state-management method and `QUALITY_SHORT` label map.
- Download button moved from full-width rectangle to 48×48 circular button inside the dock; glyph/color state machine (▶ green → ■ red → ▶ green) documented in `CLAUDE.md`.
- Filename + progress + meta relocated into the dock frame.
- Default `_set_status` colors switched from `"gray"` / `#e74c3c` to `COLOR_TEXT_MUTED` / `COLOR_DANGER`.
- Paste and Browse buttons restyled as outlined pills (transparent fg, `#7c7c7c` border).
- Cookies helper text and clip helper text dropped in favor of cleaner section labels.
- Window grown 580×530 → 620×600; `DOCK_H=92` added.
- `_toggle_clip` hides/shows the whole `_clip_row` instead of individual entries.
- `__main__`: dropped `set_default_color_theme("blue")` — all colors explicit now.
**File**: `app.py` (constants block, `App.__init__`, `_build_ui`, `_select_quality`, `_toggle_clip`, `_set_status`, `_on_close`, `_start_download`, `_reset`, `__main__`)

## FIX-027 — Palette + contrast + option descriptions pass
**Date**: 2026-04-15
**Fix**:
- **All text pure white.** Removed `COLOR_TEXT_MUTED` entirely — every text element now uses `COLOR_TEXT` (`#ffffff`). Hierarchy now comes from weight (bold vs regular) and size (11/12/13/15px), not color.
- **Inactive pills lighter for contrast.** `COLOR_INTERACT` `#1f1f1f` → `#2d2d2d` and `COLOR_HOVER` `#252525` → `#3a3a3a`. Inactive segmented pills and inputs now clearly separate from the `#121212` page bg.
- **Accent blue instead of green.** `COLOR_ACCENT` `#1ed760` → `#539df5`, hover `#1db954` → `#3d8ae0`. Applies to active segmented pill, checked checkboxes, progress bar, and circular CTA.
- **Options section restructured with descriptions.** Was a single horizontal row (H.264 / Clip / Cookies). Now vertically stacked: each option is a bold 13px label on its own line, with a regular 11px single-line description indented beneath. Clip entries sit indented under their caption, still revealed by the toggle.
- **Window grew** 620×600 → 640×700 to fit the stacked options.
- **New constant** `CAPTION_WRAP = WRAP - 28` for wrap-bounding captions under their 28px indent.
**File**: `app.py` (constants block, `_build_ui` options section)

## FIX-028 — Contrast bump + window sizing
**Date**: 2026-04-15
**Fix**:
- Inactive pills lightened further: `COLOR_INTERACT` `#2d2d2d` → `#575757` (~20% move toward white), `COLOR_HOVER` `#3a3a3a` → `#616161`. Segmented pills and inputs now clearly separate from the `#121212` page bg.
- Window shrunk 700 → 680 → 660. Closes the gap between the Cookies description and the bottom dock.
**File**: `app.py` (constants block)

## FIX-029 — App icon (source PNG → .icns + runtime iconphoto) — DEC-024
**Date**: 2026-04-15
**Fix**:
- Source: user-supplied `app_icon.png` (1073×1091) committed as the canonical icon asset.
- `make_icon.py` rewritten — loads `app_icon.png`, pads non-square input to square via `ImageOps.pad` (transparent bars), resamples at all 10 macOS iconset sizes (16/32/64/128/256/512/1024 + @2x), runs `iconutil` → `icon.icns`. Previous procedural film-strip drawing removed.
- Runtime window icon: new `App._set_window_icon()` in `__init__` loads the PNG via `tk.PhotoImage` and calls `iconphoto(True, ...)`. Best-effort — logs + swallows on failure. On macOS this sets the titlebar / minimized icon (the Dock icon during dev always shows Python).
- New resolver `_resource_path(name)` returns `sys._MEIPASS/name` when frozen, else `Path(__file__).parent/name`.
- `build.sh`: added `--add-data="app_icon.png:."` so the packaged `.app` can find the PNG for runtime iconphoto. `--icon=icon.icns` (unchanged) still drives the Dock/Finder icon.
- `.gitignore`: added `icon.icns` and `icon.iconset/` (both are deterministic outputs of `make_icon.py`).
**File**: `app.py` (imports, `_bundled_ffmpeg`/`_resource_path`, `App.__init__`, `_set_window_icon`), `make_icon.py` (full rewrite), `build.sh`

## FIX-030 — v1.2 rebrand + window centered on launch
**Date**: 2026-04-15
**Fix**:
- `APP_VERSION` `1.0.0` → `1.2`. New constant `APP_TITLE = "SAVE THIS VIDEO!"`.
- Window titlebar: `f"{APP_TITLE}  v{APP_VERSION}"`. Header label in the UI shows `APP_TITLE`.
- Centered on launch: `x = (screenW − WINDOW_W) // 2`, `y = (screenH − WINDOW_H) // 2`, clamped to ≥ 0 via `max(0, ...)`. Previously the window appeared at the OS-default position (often half off-screen at the bottom on macOS).
**File**: `app.py` (constants, `App.__init__`, `_build_ui` header label)

## FIX-031 — Code review DEC-025: all 10 demands + 15 suggestions
**Date**: 2026-04-15
**Fix**: Implemented every finding from the v1.2 code review in one pass. Key changes:
- **D1**: `_last_saved_path` shared state eliminated. Progress hook is now a closure inside `_worker()` writing to a local `saved_path[0]`. The path is passed to `_on_done(saved_path, save_dir)` as an argument via `self.after()`.
- **D2**: New `_cleanup_partial(outtmpl)` deletes `.part`/`.ytdl` files on cancel or error. Called in all three `_worker` exception paths.
- **D3+D4**: `"nooverwrites": True` added to `ydl_opts` — safety net for TOCTOU race in `_unique_outtmpl` and for probe failures that fall back to the default template.
- **D5**: `_notify()` sanitizes to printable ASCII (strips `"`, `\`, and non-ASCII) and pipes AppleScript via stdin instead of `-e` argument.
- **D6**: `_probe_info()` uses `socket_timeout: 15`. Worker checks `_cancel.is_set()` immediately after probe returns.
- **D7**: `_ask_playlist()` polls `_playlist_done` in a 0.5s loop checking `_cancel`. `_on_close()` sets both `_cancel` and `_playlist_done` to prevent deadlock.
- **D8**: `_tick()` returns immediately if `not self._downloading` — discards stale after()-queued callbacks.
- **D9**: Hook closure handles `status == "error"` with `log.warning()`.
- **D10**: CLAUDE.md build section rewritten to match unsigned `build.sh`.
- **S1**: `_parse_time` validates seconds/minutes < 60.
- **S2**: `_paste()` truncates to `MAX_URL_LEN=2048`.
- **S3**: `_paste()` calls `focus_set()` on URL entry.
- **S4**: `_set_controls_enabled(False/True)` + `_input_widgets` list disables all controls during download; re-enabled in `_reset()`.
- **S5**: `save_dir` passed as arg to `_on_done()`.
- **S6**: `_unique_outtmpl` loop capped at `n > 9999`.
- **S7+S8**: `requirements.txt` comments document curl_cffi and static-ffmpeg rationale.
- **S9**: Deleted stale `Video Downloader.spec`.
- **S11**: `_browse()` checks `os.access(path, os.W_OK)` before accepting.
- **S12**: `_wait_and_close()` logs warning on force-destroy with active worker.
- **S13**: `_probe_info()` logs exceptions at warning level.
- **S14**: `__all__ = ["App"]` added.
- **S15**: Escape key bound to cancel when downloading.
**File**: `app.py` (nearly all methods), `requirements.txt`, `CLAUDE.md`

## FIX-032 — Comment audit: replace jargon, add missing WHY comments
**Date**: 2026-04-15
**Fix**: Full comment pass per Apple code quality standards.
- **Added 11 comments** where the WHY was missing: module docstring (architecture/threading), `QUALITY_OPTIONS` (yt-dlp format syntax), `_ERROR_MAP` (order matters), `_notify.safe()` (excluded chars), `_app_icon` (Tk GC footgun), `_build_ui` grid row map, `_wait_and_close` (3s timeout rationale), `_cleanup_partial` %% unescape, `saved_path` mutable container, `status == "error"` (DASH/HLS), `nooverwrites` (TOCTOU).
- **Replaced 18 jargon tags** (`# S1:`, `# D2:`, `# D7:`, etc.) with the actual reason. Code-review IDs are internal metadata — the code itself must explain WHY.
- **Fixed 3 stale comments**: `_select_quality` "green" → "accent color", removed old review references `(D2)`, `(D9)`, `(S9)`.
**File**: `app.py`
